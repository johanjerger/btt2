#include <stdio.h>
#include "../include/btt2.h"
#include "../include/menus.h"

void
set_up_key(option_t * option, ...)
{
		key_menu(option, NULL);
}

void
set_down_key(option_t * option, ...)
{
		key_menu(option, NULL);
}

void
set_left_key(option_t * option, ...)
{
		key_menu(option, NULL);
}

void
set_right_key(option_t * option, ...)
{
		key_menu(option, NULL);
}

void
set_action_key(option_t * option, ...)
{
		key_menu(option, NULL);
}

void
set_item_key(option_t * option, ...)
{
		key_menu(option, NULL);
}
